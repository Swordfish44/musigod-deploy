'use strict'
const fixture=require('../../fixtures/contract-intelligence/tha-regime.synthetic.json');const engine=require('../../lib/expected-royalty-engine')
module.exports=async function(req,res){res.setHeader('Cache-Control','no-store');if(req.method!=='POST')return res.status(405).json({error:'POST only'});if(!process.env.ADMIN_API_KEY||req.headers['x-admin-key']!==process.env.ADMIN_API_KEY)return res.status(401).json({error:'Named administrative authorization required'});const outputs=fixture.lines.map(line=>({fixture_id:line.fixture_id,...engine.calculateLine(line)}));return res.status(200).json({fixture:{version:fixture.fixture_version,profile:fixture.profile,work:fixture.work,synthetic:true,authoritative:false,disclosures:fixture.disclosures},engine_version:engine.ENGINE_VERSION,outputs,external_submission_performed:false,live_profile_mutated:false})}
