Wire affiliate commission into admin activation + deploy dashboard
Project: MusiGod, Supabase uykzkrnoetcldeuxzqyy, deployed from C:\musigod-deploy.
Constraints (always apply):

No Supabase JS client — raw fetch with Accept-Profile / Content-Profile headers only
Service role key required for all writes
File patches via Python scripts in PowerShell
Deploy: cd C:\musigod-deploy && vercel --prod --force
Schema headers: reads use Accept-Profile, writes use Content-Profile


Task 1 — Wire fn_create_commission into admin.html activation handler
Open C:\musigod-deploy\admin.html. Find the activateOne() function (or whatever handles the Activate button click). After the existing Supabase PATCH that sets plan_status = 'ACTIVE' succeeds, add this call:
javascriptasync function triggerAffiliateCommission(artistId) {
  try {
    const artist = await fetch(
      `${SB}/rest/v1/artists_v1?id=eq.${artistId}&select=ref_code`,
      { headers: { apikey: SVC, Authorization: `Bearer ${SVC}`, 'Accept-Profile': 'artists' } }
    ).then(r => r.json());

    const refCode = artist[0]?.ref_code;
    if (!refCode) return; // artist was not referred — skip silently

    const res = await fetch(`${SB}/rest/v1/rpc/fn_create_commission`, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        apikey: SVC,
        Authorization: `Bearer ${SVC}`
      },
      body: JSON.stringify({
        p_affiliate_code: refCode,
        p_artist_id: artistId,
        p_trigger: 'admin_activation'
      })
    });
    const result = await res.json();
    console.log('[Affiliate] Commission result:', result);
  } catch (e) {
    console.warn('[Affiliate] Commission trigger failed (non-blocking):', e);
  }
}
Call await triggerAffiliateCommission(artistId) immediately after your existing activation PATCH succeeds. This must be non-blocking — if it fails, activation still completes.
Write a Python patch script to inject this, run it, verify the function appears in admin.html, then commit and deploy.

Task 2 — Deploy affiliate-dashboard.html to Vercel
The file is at C:\musigod-deploy\affiliate-dashboard.html (already in repo from previous session).

Open it and find the line: const ANON_KEY = window.SUPABASE_ANON_KEY || '__SUPABASE_ANON_KEY__';
Write a Python patch script that replaces __SUPABASE_ANON_KEY__ with the actual anon key value from the existing codebase (find it by scanning other HTML files in C:\musigod-deploy — it will be in a const ANO or const ANON variable)
Also confirm the dashboard fetch calls use Accept-Profile: affiliates on all requests to affiliates_v1, referrals_v1, commissions_v1, and affiliate_stats_v1
Commit and deploy

After deploy, verify https://musigod.com/affiliate-dashboard.html returns 200.

Task 3 — Smoke test the full live flow
After both tasks deploy:

Fetch https://musigod.com/affiliate-dashboard.html and confirm 200
Fetch https://musigod.com/admin.html and confirm 200
Do a direct POST to https://uykzkrnoetcldeuxzqyy.supabase.co/rest/v1/rpc/fn_get_commissions with p_artist_id = '3d4788b6-2a86-4ed5-8f27-ab95b3a230d3' using the service role key and confirm the commission row from the e2e test is still there with status = pending and amount = 25.0
Print a final status table: each check as PASS or FAIL

Do not attempt any Management API calls. Do not ask for a PAT. All Supabase access via service role key already in the codebase.You said: can claude code access everything that it needs?can claude code access everything that it needs?8:47 PMClaude responded: No — it will hit the same MCP wall as yesterday.No — it will hit the same MCP wall as yesterday. The Supabase MCP is authenticated to your Noterminal org, not MusiGod. So apply_migration and execute_sql are dead for this project.
What Claude Code CAN do without MCP:

Read and patch admin.html directly from disk ✅
Read and patch affiliate-dashboard.html directly from disk ✅
Find the anon key by scanning existing files ✅
Run Python patch scripts via PowerShell ✅
Commit and deploy via vercel --prod --force ✅
Smoke test live URLs via HTTP fetch ✅
POST to Supabase REST API using the service role key already in the codebase ✅